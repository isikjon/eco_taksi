import 'package:flutter/cupertino.dart';

class AppBorderRadius {
  static const top16 = BorderRadius.vertical(top: Radius.circular(16));
  static const all16 = BorderRadius.all(Radius.circular(15));
  static const all10 = BorderRadius.all(Radius.circular(10));
  static const all100 = BorderRadius.all(Radius.circular(100));
}